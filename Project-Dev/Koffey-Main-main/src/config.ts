export const API_CONFIG = {
  MODEL_CONFIG: {
    // Update this to match the exact name of your loaded model in LM Studio
    model: 'YOUR_MODEL_NAME', 
    
    // You can adjust these parameters based on your needs
    max_tokens: 2000,
    temperature: 0.7,
    top_p: 0.95,
    presence_penalty: 0,
    frequency_penalty: 0,
    stop: null,
  },
};